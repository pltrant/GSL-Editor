# Headless Game Client

`gameClient.ts` can be used during extension development for the agent to
test out in-game commands, in order to e.g. identify delimiters to match
on.

*Critical*: It is highly recommended to use a different character for the
headless CLI client than the vscode extension client.
